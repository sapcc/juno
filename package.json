{
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "apis/*",
      "libs/*"
    ]
  },
  "scripts": {
    "dashboard": "yarn workspace dashboard start",
    "ui-components": "yarn workspace juno-ui-components storybook",
    "build-libs": "NODE_ENV=development yarn workspaces foreach -p --include messages-provider --include communicator --include policy-engine --include juno-ui-components --include url-state-provider --include url-state-router --include oauth run build",
    "build": "yarn build-libs"
  },
  "dependenciesMeta": {
    "open@7.4.2": {
      "unplugged": true
    }
  },
  "dependencies": {
    "@babel/core": "^7.20.12",
    "@ibm/plex": "^5.1.3"
  }
}
