import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks"
import * as Stories from "./StyleProvider.stories"

<Meta
  title="Design System/Core/StyleProvider"
  argTypes={{
    stylesWrapper: {
      control: {
        type: "radio",
        options: ["head", "inline", "shadowRoot"],
      },
    },
  }}
/>

# StyleProvider

This component embeds the styles of this design system in an element depending on the value
of the property "stylesWrapper".

This property can contain the following values:

- **head** add style tag with css in the head element
- **inline** insert a style tag at the point where the SytleProvider is integrated.
- **shadowRoot** create a shadow root element and append it to the parent element of the StyleProvider.
  All styles are placed within this shadow root. The ShadowRoot component is used for this.

## Styles in head tag

<Canvas>
  <Story story={Stories.AddStylesToHead} name="AddStylesToHead" />
</Canvas>

## Inline Styles

<Canvas>
  <Story story={Stories.AddInlineStyles} name="AddInlineStyles" />
</Canvas>

## Shadow Root

<Canvas>
  <Story story={Stories.AddStylesToShadowRoot} name="AddStylesToShadowRoot" />
</Canvas>

## Switch Theme

<Canvas>
  <Story story={Stories.WithTheme} name="WithTheme" />
</Canvas>
