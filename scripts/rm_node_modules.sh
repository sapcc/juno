#!/bin/bash

for f in apps/**; do 
  rm -rf "$f/node_modules"
  rm -f "$f/yarn.lock" 
done
for f in lib/**; do 
  rm -rf "$f/node_modules"; 
  rm -f "$f/yarn.lock"
done
rm -rf node_modules
rm -f yarn.lock