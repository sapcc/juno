kind: Service
apiVersion: v1

metadata:
  namespace: juno
  name: hosting

spec:
  selector:
    app: hosting
  ports:
  {{- range $i, $app := .Values.apps  }}  
    - name: hosting-{{ $i }}
      port: 80
      protocol: TCP
      targetPort: hosting-{{ $i }}
  {{ end }}    
